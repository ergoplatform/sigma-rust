# This file is autogenerated by maturin v1.7.8
# To update, run
#
#    maturin generate-ci -m bindings/ergo-lib-python/Cargo.toml github
#
name: Python CI

"on":
  push:
    branches:
      - develop
      - main
      - master
    tags:
      - "ergo-lib-python-v*"
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  linux:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-22.04
            target: x86_64
          - runner: ubuntu-22.04
            target: x86
          - runner: ubuntu-22.04
            target: aarch64
          - runner: ubuntu-22.04
            target: armv7
          - runner: ubuntu-22.04
            target: s390x
          - runner: ubuntu-22.04
            target: ppc64le
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.x
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --find-interpreter --manifest-path bindings/ergo-lib-python/Cargo.toml
          sccache: "true"
          manylinux: auto
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-linux-${{ matrix.platform.target }}
          path: dist

  musllinux:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-22.04
            target: x86_64
          - runner: ubuntu-22.04
            target: x86
          - runner: ubuntu-22.04
            target: aarch64
          - runner: ubuntu-22.04
            target: armv7
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.x
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --find-interpreter --manifest-path bindings/ergo-lib-python/Cargo.toml
          sccache: "false"
          manylinux: musllinux_1_2
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-musllinux-${{ matrix.platform.target }}
          path: dist

  windows:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: windows-latest
            target: x64
          - runner: windows-latest
            target: x86
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.x
          architecture: ${{ matrix.platform.target }}
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --find-interpreter --manifest-path bindings/ergo-lib-python/Cargo.toml
          sccache: "true"
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-windows-${{ matrix.platform.target }}
          path: dist

  macos:
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: macos-15-intel
            target: x86_64
          - runner: macos-14
            target: aarch64
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.x
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --find-interpreter --manifest-path bindings/ergo-lib-python/Cargo.toml
          sccache: "true"
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-macos-${{ matrix.platform.target }}
          path: dist

  sdist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build sdist
        uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out dist --manifest-path bindings/ergo-lib-python/Cargo.toml
      - name: Upload sdist
        uses: actions/upload-artifact@v4
        with:
          name: wheels-sdist
          path: dist

  release:
    name: Release
    runs-on: ubuntu-latest
    if: ${{ (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/ergo-lib-python-v')) || github.event_name == 'workflow_dispatch' }}
    needs: [linux, musllinux, windows, macos, sdist, test]
    permissions:
      # Use to sign the release artifacts
      id-token: write
      # Used to check out the release source
      contents: read
      # Used to generate artifact attestation
      attestations: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
      - name: Verify release tag matches crate version
        if: ${{ github.event_name == 'push' && startsWith(github.ref, 'refs/tags/ergo-lib-python-v') }}
        run: |
          expected_tag="ergo-lib-python-v$(cargo metadata --no-deps --format-version 1 --manifest-path bindings/ergo-lib-python/Cargo.toml | jq -r '.packages[] | select(.name == "ergo-lib-python") | .version')"
          test "$GITHUB_REF_NAME" = "$expected_tag"
      - uses: actions/download-artifact@v4
      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v1
        with:
          subject-path: "wheels-*/*"
      - name: Publish to PyPI
        if: ${{ github.event_name == 'push' && startsWith(github.ref, 'refs/tags/ergo-lib-python-v') }}
        uses: PyO3/maturin-action@v1
        env:
          MATURIN_PYPI_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
        with:
          command: upload
          args: --non-interactive --skip-existing wheels-*/*
  test:
    name: Run python unit tests and doctests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.x
      - name: Create virtualenv
        run: "python -m venv .venv"
      - name: Maturin develop
        uses: PyO3/maturin-action@v1
        with:
          command: develop
          args: --release --manifest-path bindings/ergo-lib-python/Cargo.toml
          sccache: "true"
      - name: Install python dependencies
        working-directory: bindings/ergo-lib-python
        run: "source ../../.venv/bin/activate && pip install --require-virtualenv -r docs/requirements.txt"
      - name: Run unit tests
        working-directory: bindings/ergo-lib-python
        run: "python -m unittest tests/*.py"
      - name: Test stubs
        working-directory: bindings/ergo-lib-python
        run: "source ../../.venv/bin/activate && stubtest ergo_lib_python --allowlist .mypy_allowlist.txt"
      - name: Type-check constructor signatures
        working-directory: bindings/ergo-lib-python
        run: "source ../../.venv/bin/activate && python -m mypy --warn-unused-ignores tests/typing/ergo_box_candidate_constructor.py"
      - name: Run doctests
        working-directory: bindings/ergo-lib-python
        run: "source ../../.venv/bin/activate && cd docs && make doctest"
